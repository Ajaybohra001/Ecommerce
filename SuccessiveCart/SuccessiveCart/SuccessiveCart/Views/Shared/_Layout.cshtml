@using Microsoft.AspNetCore.Identity
@using SuccessiveCart.Models.Domain
@inject SignInManager<Users> sign
@inject UserManager<Users> userManager

@{
    var CategoryList = ViewBag.CategoryList as List<SuccessiveCart.Models.Domain.Cateogry>;
    var UserList = ViewBag.UserList as List<SuccessiveCart.Models.Domain.Users>;

}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SuccessiveCart</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SuccessiveCart.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="//cdn.datatables.net/1.13.7/css/jquery.dataTables.min.css"/>
    
        <!-- Required meta tags -->
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <link rel="icon" href="img/favicon.png" type="image/png" />
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
        <title>SuccessiveCart</title>
        <style>
        .filter-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

            .filter-section > div {
                flex: 1;
                margin-right: 10px;
            }

            .filter-section select,
            .filter-section input[type="number"],
            .filter-section input[type="text"] {
                width: 100%;
                margin-bottom: 10px;
                padding: 8px;
                border-radius: 5px;
                border: 1px solid #ccc;
                box-sizing: border-box;
            }

        .product-row {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
        }

        .product-card {
            border: 1px solid #ddd;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            width: calc(25% - 20px);
            margin: 10px;
            box-sizing: border-box;
        }

            .product-card:hover {
                box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
                transform: translateY(-5px);
            }

        .product-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 10px 10px 0 0;
        }

        .product-details {
            padding: 15px;
            text-align: center;
        }

        .product-name {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .product-price {
            font-size: 14px;
            color: crimson;
            margin-bottom: 10px;
        }

        .product-actions {
            display: flex;
            justify-content: space-around;
            margin-top: 10px;
        }

        /* Adjust button styles */
        .favorite-btn,
        .add-to-cart-btn {
            padding: 8px 20px;
            border-radius: 5px;
        }

            .favorite-btn:hover,
            .add-to-cart-btn:hover {
                background: #0056b3;
            }

        .heading-center {
            text-align: center;
            margin-bottom: 20px;
        }
        </style>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>

        function filterProducts() {
            var categoryFilter = document.getElementById("categoryFilter").value;
            var minPrice = parseFloat(document.getElementById("minPrice").value);
            var maxPrice = parseFloat(document.getElementById("maxPrice").value);

            $(".product-card").each(function () {
                var productCategory = $(this).data("category");
                var productPrice = parseFloat($(this).data("price"));
                var isTrending = $(this).hasClass("trending-product");

                var showProduct = ((!categoryFilter || productCategory == categoryFilter) &&
                    (!minPrice || productPrice >= minPrice) &&
                    (!maxPrice || productPrice <= maxPrice)) &&
                    (!isTrending);

                $(this).toggle(showProduct);
            });

            if (categoryFilter.trim() === "" && isNaN(minPrice) && isNaN(maxPrice)) {
                $(".trending-products").show();
            } else {
                $(".trending-products").hide();
            }
        }
        function searchProducts() {
            var searchText = $("#searchInput").val().toLowerCase();
            var selectedCategory = $("#categoryFilter").val();

            $(".product-card").each(function () {
                var productName = $(this).find(".product-name").text().toLowerCase();
                var productCategory = $(this).data("category").toString();
                var isTrending = $(this).hasClass("trending-product");

                var matchSearch = productName.includes(searchText);
                var matchCategory = selectedCategory === "" || productCategory === selectedCategory;

                var showProduct = matchSearch && matchCategory && (!isTrending);

                $(this).toggle(showProduct);
            });

            if (searchText.trim() === "") {
                $(".trending-products").show();
            } else {
                $(".trending-products").hide();
            }
        }






    </script>

        
        <link rel="stylesheet" href="css/responsive.css" />
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var selectCategoryLinks = document.querySelectorAll(".select-category");
            selectCategoryLinks.forEach(function (link) {
                link.addEventListener("click", function () {
                    var categoryId = this.getAttribute("data-category-id");
                    document.getElementById("categoryIdInput").value = categoryId;
                });
            });
        });
    </script>


      <style>
        .bgaqua {
            background: #4549df;
        }

        .bglightblue {
            background: #fbfbfb;
        }

        .fullwidth {
            max-width: 100%;
        }

        .bggrey {
            background-color: #f2ebeb;
        }

        .ofxh {
            overflow-y: hidden;
        }

        </style >
       
</head>
<body>

    @if(sign.IsSignedIn(User))
    {
        var id = userManager.GetUserId(User);

        @if(User.IsInRole("Admin"))
        {
            
                <nav class="navbar navbar-expand-lg d-flex justify-content-between bgaqua w-100">
                    <div class="container-fluid">
                    <a class="navbar-brand text-white" asp-action="AdminDashboard" asp-controller="Login">Admin Dashboard</a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav me-auto">
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle text-white" id="usersDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        Manage Users
                                    </a>
                                    <ul class="dropdown-menu" aria-labelledby="usersDropdown">
                                        <li><a class="dropdown-item" asp-action="UsersList" asp-controller="Admin">All Users</a></li>
                                        <li><a class="dropdown-item" asp-action="AddUser" asp-controller="Admin">Add New User</a></li>
                                    </ul>
                                </li>

                               






                            <li class="nav-item">
                                <a class="nav-link text-white" asp-action="CateogryList" asp-controller="Admin">Manage Categories</a>
                            </li>






                            <li class="nav-item dropdown" >
                                <a class="nav-link dropdown-toggle text-white" href="#" id="categoryDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Category
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="usersDropdown" >
                                    @if (CategoryList != null && CategoryList.Count > 0)
                                    {
                                        foreach (var x in CategoryList)
                                        {
                                            <li>

                                                <a class="dropdown-item" onclick="selectCategory('@x.CateogryId', '@x.CateogryName')" value="@x.CateogryId">@x.CateogryName</a>
                                            </li>
                                        }
                                    }
                                </ul>
                            </li>


                            </ul>
                            <ul class="navbar-nav mt-md-4 mt-lg-0">
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <span class="fs-4"> Hello </span><span class="fst-italic fw-bold fs-4">@User.Identity!.Name</span>
                                    </a>
                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" href="/UserProfile/Index/@id">Profile</a></li>

                                        <li><a class="dropdown-item" asp-action="Logout" asp-controller="Verification">Logout</a></li>
                                    </ul>
                                </li>
                            </ul>
                        </div>
                    </div>
            </nav>
           
  

               

                @section Scripts {
            <script src="~/js/product.js"></script>
                }
                
        }
    }
    
    @if(sign.IsSignedIn(User))
    {
        var id = userManager.GetUserId(User);
        @if (User.IsInRole("User"))
        {
            <nav class="navbar navbar-expand-lg d-flex justify-content-between bgaqua w-100 mt-0    ">
                <div class="container-fluid">
                    <a class="navbar-brand text-white" asp-action="" asp-controller="">Successive Cart</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav me-auto">


                           








                            <li class="nav-item">
                                <a class="nav-link text-white" asp-action="MyCart" asp-controller="Cart">My Cart</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link text-white" asp-action="AllProducts" asp-controller="Cart">All Products</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-white" asp-action="FavouriteProducts" asp-controller="Favourite">Favourite Products</a>
                            </li>




                        </ul>
                        <ul class="navbar-nav mt-md-4 mt-lg-0">
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <span class="fs-4"> Hello </span><span class="fst-italic fw-bold fs-4">@User.Identity!.Name</span>
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" href="/UserProfile/Index/@id">Profile</a></li>

                                    <li><a class="dropdown-item" asp-action="Logout" asp-controller="Verification">Logout</a></li>
                                </ul>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>

            @section Scripts {
        <script src="~/js/product.js"></script>
            }
            
        }

    }

    else
    {
        <nav class="navbar navbar-expand-lg d-flex justify-content-between bgaqua w-100">
            <div class="container-fluid">
                <a class="navbar-brand text-white" asp-action="" asp-controller="">Successive Cart</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                    <ul class="navbar-nav mt-md-4 mt-lg-0">

                        <li class="nav-item mt-md-4 mt-lg-0" >
                            <a class="nav-link text-white" asp-action="Login" asp-controller="Verification">Login</a>
                        </li>
                        <li class="nav-item mt-md-4 mt-lg-0">
                            <a class="nav-link text-white" asp-action="Register" asp-controller="Verification">Register</a>
                        </li>

                     


                    </ul>
                   
                </div>
            </div>
        </nav>

     



    }

   
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>


    </div>

   @*  <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2024 - SuccessiveCart - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer> *@
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="//cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js" asp-append-version="true"></script>
    <script>
        if (performance.navigation.type == 2) {
            location.reload();
        }
    </script>

    @await RenderSectionAsync("Scripts", required: false)

    @functions {
        private bool IsCurrentPage(string controller)
        {
            var routeData = ViewContext.RouteData.Values;
            var currentController = routeData["Controller"] as string ?? "";

            return string.Equals(currentController, controller, StringComparison.OrdinalIgnoreCase);
        }
    
    }
</body>
</html>
